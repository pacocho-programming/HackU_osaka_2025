specVersion: "v1beta"
serviceId: "main"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "main-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
