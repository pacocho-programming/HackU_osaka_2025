specVersion: "v1beta"
serviceId: "map"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "map-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
